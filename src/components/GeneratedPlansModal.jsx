import React from 'react'

const GeneratedPlansModal = ({isOpen}) => {
    return (
        <div className="fixed inset-0 bg-black/40 backdrop-blur-sm z-50 flex items-center justify-center px-4">
            Hello
        </div>
    )
}

export default GeneratedPlansModal
